// Your code here.
function min(numA, numB) {
  return (numA < numB ? numA : numB);
};

console.log(min(0, 10));
// → 0
console.log(min(0, -10));
// → -10

+---------------------

function min(numA, numB) {
  return numA < numB ? numA : numB;
};
